type: object
properties:
  latitude:
    type: number
    format: double
    description: the latitude of a location
  longitude:
    type: number
    format: double
    description: the longitude of a location
